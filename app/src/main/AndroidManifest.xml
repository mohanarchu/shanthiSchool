<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:tx="http://schemas.android.com/apk/res-auto"
    package="com.video.aashi.school">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />


    <application
        android:name=".adapters.MyApp"
        android:allowBackup="true"
        android:configChanges="keyboardHidden|orientation"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Payment" />
        <activity android:name=".Myclass" />
        <activity
            android:name=".NoticeDetails"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".adapters.Successful" />
        <activity android:name=".ForgetPass" />
        <activity android:name=".Navigation" />
        <activity android:name=".Login" />
        <activity android:name=".BarScanner" />
        <activity android:name=".PinLogin" />
        <activity android:name=".pinchange.ChangePin" />
        <activity android:name=".fragments.studentlist.StudentList" />
        <activity android:name=".LoginAttempt"></activity>
    </application>

</manifest>